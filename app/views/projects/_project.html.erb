 <div class="well infobox" onclick="document.location='/projects/<%= project.id %>'">
    
     
	 
     
     <p align="right"><i><%= project.subtitle %></i></p>
	 
	 
	      <h2><%= project.name %>
		  
		  <% if project.name.include? "Bloodwork" %>
		  	 &nbsp;<a class="btn btn-default btn-fab"><i class="material-icons">opacity</i></a>
		  	 <% end %>
<% if project.name.include? "EKG" %>
	 &nbsp;<a class="btn btn-default btn-fab">
		 <!-- <i class="fa fa-heartbeat" style= "margin:18px"></i> -->
	 <i class="material-icons">favorite</i>
	 </a>
	 <% end %>
<% if project.name.include? "CXR" %>
	 &nbsp;<a class="btn btn-default btn-fab"><i class="material-icons">filter_b_and_w</i></a>
	 <% end %>	 
<% if project.name.include? "Consent" %>
	 &nbsp;<a class="btn btn-default btn-fab"><i class="material-icons">content_paste</i></a>
	 <% end %>	
	 <% if project.name.include? "Medical" %>
	 	 &nbsp;<a class="btn btn-default btn-fab"><i class="material-icons">beenhere</i></a>
	 <% end %>		 
	 <% if project.name.include? "Skills" %>
	 	 &nbsp;<a class="btn btn-default btn-fab"><i class="material-icons">healing</i></a>
	 <% end %>
	 <% if project.name.include? "Social Work" %>
	 	 &nbsp;<a class="btn btn-default btn-fab"><i class="material-icons">leak_add</i></a>
	 <% end %>

	 </h2>
     <p>
     <!-- <% project.user_ids.each do |uid| %>
	 	<% each_user = User.find(uid) %>
		<% if each_user.usertype != "0" %>
		<% if each_user.usertype == "1" %>
		<%= each_user.name %> <font size="1px"><i>
			<% if each_user.school != "0" %>
				<% if each_user.school == "1" %>Engineering
				<% elsif each_user.school == "1" %>Med
				<% elsif each_user.school == "1" %>Public
				<% end %>
			<% end %></i></font> -->
		
		
		
		<!-- <%elsif each_user.usertype == "2" %>
			<%= each_user.name %>
			<% if each_user.title != 0 %>
				<% if each_user.title == "1" %>,&nbsp;MD
				<% elsif each_user.title == "2" %>,&nbsp;NP
				<% elsif each_user.title == "3" %>,&nbsp;RN
				<% else %>
				<% end %>
			<% end %>
		<%end%>
		<%end%>
     	;&nbsp;
	 <% end %>
     </p> -->
     <p>
     <%= project.description %> 
     </p>
     
   
 </div>